name: spellchecker
description: A privacy-first, open-source Flutter spell checker and writing assistant.
publish_to: "none"
version: 3.1.0+23

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
