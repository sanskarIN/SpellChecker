# V2.14 Working Scope

This branch implements the ninth built-in writing rule: advisory unmatched square-bracket diagnostics.

The permanent release target is `2.14.0+19` / About `2.14.0`. The rule ID is `unmatched-square-bracket`, the public class is `UnmatchedSquareBracketRule`, and the compatibility boundary preserves explicit V2.13 eight-rule overrides while unset/reset preferences adopt the current nine-rule registry defaults.

This working scope is intentionally concise; final behavior, compatibility, validation, release evidence, and file-level changes are recorded in the dedicated V2.14 behavior/validation documents and `what_changed.md` before merge.
