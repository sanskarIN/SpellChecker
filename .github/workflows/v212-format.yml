name: V2.12 one-time formatter

on:
  push:
    branches:
      - v2.12-missing-punctuation-space-2026-08-15

permissions:
  contents: write

concurrency:
  group: v212-one-time-formatter
  cancel-in-progress: true

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out V2.12 branch
        uses: actions/checkout@v7
        with:
          ref: v2.12-missing-punctuation-space-2026-08-15
          fetch-depth: 0

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Canonically format tracked Dart
        shell: bash
        run: |
          set -euo pipefail
          dart format lib test tool
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- lib test tool; then
            git add lib test tool
            git commit -m "style: format V2.12 implementation and regressions"
          fi
          git rm .github/workflows/v212-format.yml
          git commit -m "ci: remove one-time V2.12 formatter"
          git push origin HEAD:v2.12-missing-punctuation-space-2026-08-15
