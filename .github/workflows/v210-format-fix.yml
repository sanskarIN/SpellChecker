name: V2.10 formatter repair

on:
  push:
    branches:
      - v2.10-deterministic-benchmarks

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out V2.10 branch
        uses: actions/checkout@v7
        with:
          ref: v2.10-deterministic-benchmarks
          fetch-depth: 0

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Format and commit exact CI-reported files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          format_one() {
            path="$1"
            message="$2"
            dart format "$path"
            if ! git diff --quiet -- "$path"; then
              git add "$path"
              git commit -m "$message"
            fi
          }

          format_one test/analysis_benchmark_options_test.dart "style: format benchmark option tests"
          format_one test/analysis_benchmark_result_test.dart "style: format benchmark result tests"
          format_one test/analysis_benchmark_runner_test.dart "style: format benchmark runner tests"
          format_one tool/benchmark/analysis_benchmark_result.dart "style: format benchmark result models"

          git push origin HEAD:v2.10-deterministic-benchmarks
