name: Apply V2.0 writing rules integration

on:
  push:
    branches:
      - v2.0-writing-rules-foundation
    paths:
      - .github/workflows/v20-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out latest V2.0 branch
        uses: actions/checkout@v4
        with:
          ref: v2.0-writing-rules-foundation
          fetch-depth: 0

      - name: Apply guarded editor integration
        run: python3 tools/v20_apply.py

      - name: Remove one-time helper
        run: |
          rm -f tools/v20_apply.py
          rm -f .github/workflows/v20-apply.yml

      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No V2.0 integration changes to commit."
            exit 0
          fi
          git commit -m "feat: integrate local writing insights into editor"
          git pull --rebase origin v2.0-writing-rules-foundation
          git push origin HEAD:v2.0-writing-rules-foundation
